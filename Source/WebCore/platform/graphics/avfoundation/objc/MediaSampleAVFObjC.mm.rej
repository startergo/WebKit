diff a/Source/WebCore/platform/graphics/avfoundation/objc/MediaSampleAVFObjC.mm b/Source/WebCore/platform/graphics/avfoundation/objc/MediaSampleAVFObjC.mm	(rejected hunks)
@@ -35,6 +35,12 @@
 #import <pal/cf/CoreMediaSoftLink.h>
 #import "CoreVideoSoftLink.h"
 
+// [leopard-webkit-build] Gate under USE(AVFOUNDATION) — this TU uses CoreMedia
+// functions/types (CMSampleBuffer*, CMVideoFormatDescription*) that are only
+// available via the AVF/CoreMedia soft-link path. On 10.6 where USE(AVFOUNDATION)=0
+// the body compiles out (the class declaration in the .h is sufficient for callers).
+#if ENABLE(VIDEO) && USE(AVFOUNDATION)
+
 using namespace PAL;
 
 WTF_DECLARE_CF_TYPE_TRAIT(CMSampleBuffer);
