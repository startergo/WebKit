diff a/Source/WebCore/platform/mac/WebCoreFullScreenPlaceholderView.mm b/Source/WebCore/platform/mac/WebCoreFullScreenPlaceholderView.mm	(rejected hunks)
@@ -24,7 +24,10 @@
  */
 
 #include "config.h"
-#if !PLATFORM(IOS)
+// [leopard-webkit-build] NSVisualEffectView (base of the fullscreen placeholder) is
+// 10.10+. Gate the whole TU out on < 10.10 — the fullscreen placeholder is a
+// visual-only element shown during fullscreen transitions.
+#if !PLATFORM(IOS) && __MAC_OS_X_VERSION_MIN_REQUIRED >= 101000
 
 #include "WebCoreFullScreenPlaceholderView.h"
 
